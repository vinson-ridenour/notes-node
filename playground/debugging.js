var person = {
    name: 'Vinse'
};

person.age = 25;

debugger; // use c here in the debugger console to get to this point, then c again to run through rest of program

person.name = 'Mike';

console.log(person);